<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
    "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  NetworkUSB usbmuxd-agent LaunchDaemon (template).

  Runs at boot (as root), keeps the agent alive, restarts on crash.
  The shared secret is read from a root-owned 0600 file via --token-file,
  NOT stored in this plist (see PROBLEMS.md F-04).
  Logging is handled by the agent itself -> /var/log/usbmuxd-agent.log.

  Set __NETWORKUSB_AGENT_BIN__ to the absolute path of the installed agent
  (e.g. /Users/admin/NetworkUSB/.venv/bin/usbmuxd-agent) and keep
  /etc/networkusb/token containing the secret (root:wheel 0600).

  Install:
    sudo cp com.usbmuxd.agent.plist /Library/LaunchDaemons/
    sudo chown root:wheel /Library/LaunchDaemons/com.usbmuxd.agent.plist
    sudo chmod 644 /Library/LaunchDaemons/com.usbmuxd.agent.plist
    sudo launchctl bootstrap system /Library/LaunchDaemons/com.usbmuxd.agent.plist

  Uninstall:
    sudo launchctl bootout system/com.usbmuxd.agent
    sudo rm /Library/LaunchDaemons/com.usbmuxd.agent.plist
-->
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.usbmuxd.agent</string>

    <key>ProgramArguments</key>
    <array>
        <string>__NETWORKUSB_AGENT_BIN__</string>
        <string>--host</string>
        <string>0.0.0.0</string>
        <string>--port</string>
        <string>8721</string>
        <string>--token-file</string>
        <string>/etc/networkusb/token</string>
        <string>--log-level</string>
        <string>INFO</string>
    </array>

    <key>RunAtLoad</key>
    <true/>

    <key>KeepAlive</key>
    <true/>

    <key>ProcessType</key>
    <string>Background</string>

    <!-- Throttle restarts (also avoids tight crash loops). -->
    <key>ThrottleInterval</key>
    <integer>5</integer>
</dict>
</plist>
